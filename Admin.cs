namespace CoachingMangementSystem
{
    internal class Admin : Person
    {
        internal Admin(
            string id,
            string name,
            string username,
            string password)
            : base(id, name, username, password)
        {
        }
    }
}
